import React from 'react';

export default function Logo() {
    return (
      <span className="font-display text-lg tracking-tight whitespace-nowrap">
        <span className="logo-accent">&gt;_</span>
        <span className="logo-primary">console.</span>
        <span className="logo-accent">log</span>
        <span className="logo-primary">(</span>
        <span className="logo-muted">ic</span>
        <span className="logo-primary">)</span>
      </span>
    );
  }
  